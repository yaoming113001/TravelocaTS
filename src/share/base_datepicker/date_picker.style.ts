import { StyleSheet } from "react-native"

export default StyleSheet.create({
  overPlay: { borderRadius: 10 },
  container: { width: 300, alignItems: "center" },
  title: { textAlign: "center", fontSize: 20, fontWeight: "bold", marginBottom: 40 },
  calender: { alignItems: "center", justifyContent: "center" },
  button: { width: 100 }
}) 